language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-passport-auth' ]; then cd .. && eval "mv $currentfolder generator-passport-auth" && cd generator-passport-auth; fi

